import assert from 'node:assert/strict';
import test from 'node:test';
import {createHash} from 'node:crypto';
import {pdfQuizLimits,readUploadedPdfFile} from '../api/_lib/pdf-quiz-source.js';
function escapePdfText(value){return String(value).replace(/([\\()])/g,'\\$1')}
function makePdf(text='PDF quiz source test'){const stream=text?`BT /F1 14 Tf 72 720 Td (${escapePdfText(text)}) Tj ET`:'';const objects=['<< /Type /Catalog /Pages 2 0 R >>','<< /Type /Pages /Kids [3 0 R] /Count 1 >>','<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources << /Font << /F1 4 0 R >> >> /Contents 5 0 R >>','<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>',`<< /Length ${Buffer.byteLength(stream)} >>\nstream\n${stream}\nendstream`];let out='%PDF-1.4\n',offsets=[0];objects.forEach((body,index)=>{offsets[index+1]=Buffer.byteLength(out);out+=`${index+1} 0 obj\n${body}\nendobj\n`});const xref=Buffer.byteLength(out);out+='xref\n0 6\n0000000000 65535 f \n';for(let i=1;i<=5;i++)out+=`${String(offsets[i]).padStart(10,'0')} 00000 n \n`;out+=`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF\n`;return Buffer.from(out,'ascii')}
const b64=buffer=>buffer.toString('base64');

test('PDF compatibility extraction stays deterministic and bounded',async()=>{assert.deepEqual(pdfQuizLimits,{maxBytes:2_000_000,maxPages:80,maxSourceText:400_000});const buffer=makePdf('PDF quiz source test'),read=await readUploadedPdfFile('lesson.pdf',b64(buffer));assert.equal(read.file.mimeType,'application/pdf');assert.equal(read.pdfPages,1);assert.ok(read.text.includes('PDF quiz source test'));assert.equal(read.sourceHash,createHash('sha256').update(buffer).digest('hex'))});
test('PDF compatibility parser fails closed',async()=>{await assert.rejects(readUploadedPdfFile('fake.pdf',Buffer.from('not a pdf').toString('base64')),/chữ ký PDF/i);await assert.rejects(readUploadedPdfFile('scan.pdf',b64(makePdf(''))),/không có lớp văn bản|không OCR/i)});
test('PDF is not an addressable path in the canonical Phase 19 bank Update',async()=>{const fs=await import('node:fs');const api=fs.readFileSync('api/_lib/quiz-pipeline-v2.js','utf8'),learning=fs.readFileSync('src/components/admin/LearningContentManagerPanel.tsx','utf8'),ingest=fs.readFileSync('api/_lib/trusted-quiz-ingest.js','utf8'),pkg=JSON.parse(fs.readFileSync('package.json','utf8'));assert.equal(pkg.dependencies['pdf-parse'],'2.4.5');assert.ok(api.includes('readUploadedPdfFile'),'legacy compatibility pipeline may retain bounded PDF support');assert.ok(!learning.includes('.pdf')&&!learning.includes('sourceFileBase64'),'canonical bank manager must not expose PDF upload');assert.ok(ingest.includes('rows.filter(isConvertibleDocument)')&&!ingest.includes('PDFParse'),'canonical trusted bank fast path must remain red-answer document only and exclude PDF')});
