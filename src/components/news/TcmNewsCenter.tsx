import { useEffect,useState } from 'react';import { ExternalLink,RefreshCw,ShieldCheck } from 'lucide-react';import type { Member } from '../../types';import { roleAtLeast } from '../../types';import { supabase } from '../../services/authService';import { HerbIcon } from '../icons/YhctIcons';
type News={id:string;title:string;canonical_url:string;publisher:string;publisher_domain:string;published_at?:string|null;summary:string;tags:string[];trust_score:number;ai_provider:string};
export default function TcmNewsCenter({member}:{member:Member|null}){const [items,setItems]=useState<News[]>([]),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const canReview=roleAtLeast(member?.role,'mod');const load=async()=>{setBusy(true);setMsg('');try{const {data,error}=await supabase.rpc('tcm_news_feed_v1',{p_limit:40});if(error)throw error;setItems((data||[]) as News[])}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};useEffect(()=>{void load()},[]);return <section><div className="between"><div className="row panel-title"><HerbIcon/><div><h2>Tin tức Y học cổ truyền</h2><p>Tổng hợp nguồn RSS công khai · tóm tắt ngắn · luôn giữ liên kết nguồn gốc.</p></div></div><button className="secondary" disabled={busy} onClick={()=>void load()}><RefreshCw/>Làm mới</button></div>{msg&&<div className="error">{msg}</div>}{items.length===0?<section className="panel empty-state"><HerbIcon/><h3>Chưa có tin đã qua ngưỡng tin cậy</h3><p>Pipeline vẫn thu thập định kỳ; nội dung chưa đủ điểm tin cậy được giữ ở hàng chờ thay vì tự động công bố.</p></section>:<div className="news-grid">{items.map(n=><article className="news-card" key={n.id}><div className="news-meta"><span>{n.publisher||n.publisher_domain||'Nguồn tổng hợp'}</span><span>{n.published_at?new Date(n.published_at).toLocaleString('vi-VN'):'Chưa rõ thời gian'}</span></div><h3>{n.title}</h3><p>{n.summary}</p><div className="tags">{(n.tags||[]).map(t=><span key={t}>#{t}</span>)}</div><footer><span className="trust">Tin cậy {Math.round(Number(n.trust_score||0)*100)}%</span><a href={n.canonical_url} target="_blank" rel="noreferrer noopener">Đọc nguồn <ExternalLink/></a></footer></article>)}</div>}{canReview&&<div className="ai-note"><ShieldCheck/> Tài khoản quản trị có quyền duyệt nguồn; pipeline chỉ tự công bố nội dung vượt ngưỡng tin cậy.</div>}</section>}
