import { useEffect,useState } from 'react';import { Activity,BrainCircuit,CloudSun,DatabaseBackup,Download,RefreshCw,RotateCcw,ShieldAlert,Trash2 } from 'lucide-react';import { supabase } from '../../services/authService';import { formatLunar } from '../../utils/lunar';import { TaijiIcon } from '../icons/YhctIcons';
type Snapshot={id:string;scope:string;checksum_sha256:string;row_counts:Record<string,number>;created_at:string};type Audit={id:string;action:string;severity:string;entity_type?:string|null;entity_id?:string|null;metadata:Record<string,unknown>;created_at:string};type Health={ok:boolean;serverTime:string;postgres:string;counts:Record<string,number>;recentErrors:unknown[]};type Weather={available:boolean;temperature?:number;apparentTemperature?:number;humidity?:number;windSpeed?:number;condition?:string;locationMode?:string};
const downloadJson=(name:string,data:unknown)=>{const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.rel='noopener';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};
export default function SystemAdminCenter(){const [health,setHealth]=useState<Health|null>(null),[snaps,setSnaps]=useState<Snapshot[]>([]),[logs,setLogs]=useState<Audit[]>([]),[weather,setWeather]=useState<Weather|null>(null),[diag,setDiag]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const now=new Date();const load=async()=>{setBusy(true);setMsg('');try{const [h,s,l]=await Promise.all([supabase.rpc('acc_health_v1'),supabase.rpc('acc_list_snapshots_v1',{p_limit:20}),supabase.rpc('acc_recent_logs_v1',{p_limit:80})]);if(h.error)throw h.error;if(s.error)throw s.error;if(l.error)throw l.error;setHealth(h.data as Health);setSnaps((s.data||[]) as Snapshot[]);setLogs((l.data||[]) as Audit[])}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};const loadWeather=async(lat?:number,lon?:number)=>{try{const qs=Number.isFinite(lat)&&Number.isFinite(lon)?`?lat=${lat}&lon=${lon}`:'';const r=await fetch(`/api/weather${qs}`,{cache:'no-store'});setWeather(await r.json())}catch{setWeather({available:false})}};useEffect(()=>{void load();void loadWeather()},[]);
const createSnapshot=async()=>{setBusy(true);try{const {data,error}=await supabase.rpc('acc_create_snapshot_v1');if(error)throw error;setMsg(`Snapshot đã tạo · checksum ${(data as any).checksum}`);await load()}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};const exportSnapshot=async(id:string)=>{setBusy(true);try{const {data,error}=await supabase.rpc('acc_export_snapshot_v1',{p_snapshot_id:id});if(error)throw error;downloadJson(`yhct-managed-snapshot-${id}.json`,data);setMsg('Đã xuất JSON cục bộ kèm checksum. File có thể chứa dữ liệu quản trị; chỉ lưu ở nơi kiểm soát truy cập.')}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};const restore=async(s:Snapshot)=>{if(window.prompt(`Nhập RESTORE để khôi phục snapshot ${s.id.slice(0,8)}.`)!=='RESTORE')return;setBusy(true);try{const {data,error}=await supabase.rpc('acc_restore_snapshot_v1',{p_snapshot_id:s.id});if(error)throw error;setMsg(`Restore hoàn tất và đã kiểm tra số dòng. Pre-restore snapshot: ${(data as any).preRestoreSnapshotId}`);await load()}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};const purge=async()=>{try{const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('yhct-hiu-4-')).map(k=>caches.delete(k)));const reg=await navigator.serviceWorker?.getRegistration();await reg?.update();setMsg(`Đã purge ${keys.length} cache client. Vercel serverless không cần restart thủ công.`)}catch(e){setMsg((e as Error).message)}};const diagnose=async()=>{setBusy(true);setDiag('');try{const {data:{session}}=await supabase.auth.getSession();if(!session?.access_token)throw new Error('Phiên Admin đã hết hạn. Hãy đăng nhập lại.');const r=await fetch('/api/ai/diagnostics',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${session.access_token}`},body:JSON.stringify({health,logs:logs.slice(0,40)})});if(!r.ok){const j=await r.json().catch(()=>({}));throw new Error(j.error||`Diagnostic HTTP ${r.status}`)}const j=await r.json();setDiag(j.analysis||'Không có kết quả.')}catch(e){setDiag((e as Error).message)}finally{setBusy(false)}};const gps=()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>void loadWeather(p.coords.latitude,p.coords.longitude),e=>setMsg(`Không lấy được GPS: ${e.message}`),{timeout:7000,maximumAge:600000}):setMsg('Thiết bị không hỗ trợ GPS trình duyệt.');
return <section><div className="between"><div className="row panel-title"><ShieldAlert/><div><h2>Admin Control Center · System</h2><p>Admin-only · snapshot checksum · health/log/diagnostic · audit trail.</p></div></div><button className="secondary" disabled={busy} onClick={()=>void load()}><RefreshCw/>Làm mới</button></div>{msg&&<div className="ai-note">{msg}</div>}<div className="acc-grid"><section className="panel acc-status"><div className="row"><TaijiIcon/><div><b>{now.toLocaleDateString('vi-VN',{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'})}</b><small>Âm lịch Việt Nam: {formatLunar(now)}</small></div></div><div className="row"><CloudSun/><div><b>{weather?.available?`${weather.temperature}°C · ${weather.condition}`:'Thời tiết chưa xác định'}</b><small>{weather?.available?`Cảm giác ${weather.apparentTemperature}°C · Ẩm ${weather.humidity}% · Gió ${weather.windSpeed} km/h · ${weather.locationMode}`:'Dùng GPS thiết bị để tăng độ chính xác.'}</small></div></div><button className="secondary" onClick={gps}>Dùng GPS thiết bị</button></section><section className="panel"><div className="row"><Activity/><h3>System Health</h3></div>{health&&<><div className="health-kpis">{Object.entries(health.counts||{}).map(([k,v])=><span key={k}><b>{v}</b><small>{k}</small></span>)}</div><p className="muted">Postgres {health.postgres} · {new Date(health.serverTime).toLocaleString('vi-VN')}</p></>}<div className="schedule-actions"><button onClick={()=>void purge()}><Trash2/>Purge cache</button><button className="secondary" disabled={busy} onClick={()=>void diagnose()}><BrainCircuit/>A.I Diagnostic</button></div>{diag&&<pre className="diagnostic-output">{diag}</pre>}</section></div><section className="panel"><div className="between"><div className="row"><DatabaseBackup/><div><h3>Snapshot & Restore</h3><p className="muted">Logical snapshot các module app-managed; không thay thế physical backup PostgreSQL. Restore được khóa giao dịch, kiểm SHA-256 và xác minh lại số dòng.</p></div></div><button disabled={busy} onClick={()=>void createSnapshot()}><DatabaseBackup/>Tạo snapshot</button></div><div className="data-list">{snaps.map(s=><article key={s.id}><div><b>{new Date(s.created_at).toLocaleString('vi-VN')}</b><small>SHA-256 {s.checksum_sha256.slice(0,18)}…</small></div><div className="schedule-actions"><button className="secondary" onClick={()=>void exportSnapshot(s.id)}><Download/>JSON</button><button className="danger-btn" onClick={()=>void restore(s)}><RotateCcw/>Restore</button></div></article>)}</div></section><section className="panel"><div className="row"><ShieldAlert/><h3>Audit & Error Logs</h3></div><div className="log-list">{logs.map(x=><article key={x.id} className={`log-${x.severity}`}><div><b>{x.action}</b><small>{new Date(x.created_at).toLocaleString('vi-VN')} · {x.entity_type||'system'} {x.entity_id||''}</small></div><span>{x.severity}</span></article>)}</div></section></section>}
