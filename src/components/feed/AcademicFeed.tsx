import { useMemo,useState } from 'react';import { Hash,Search } from 'lucide-react';import type { AcademicPost } from '../../types';import AcademicPostCard,{academicScore} from './AcademicPostCard';
export default function AcademicFeed({posts,setPosts}:{posts:AcademicPost[];setPosts:React.Dispatch<React.SetStateAction<AcademicPost[]>>}){const [q,setQ]=useState('');const [tag,setTag]=useState('');const tags=useMemo(()=>[...new Set(posts.flatMap(p=>p.tags))],[posts]);const shown=useMemo(()=>posts.filter(p=>(!tag||p.tags.includes(tag))&&(!q||`${p.title} ${p.syndrome} ${p.tags.join(' ')}`.toLowerCase().includes(q.toLowerCase()))).sort((a,b)=>academicScore(b)-academicScore(a)),[posts,q,tag]);return <section><div className="feed-tools"><div className="searchbox"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm y án, thể bệnh, hashtag…"/></div><div className="chips"><button className={!tag?'active':''} onClick={()=>setTag('')}>Tất cả</button>{tags.map(t=><button className={tag===t?'active':''} key={t} onClick={()=>setTag(t)}><Hash/>{t}</button>)}</div></div>{shown.map(p=><AcademicPostCard key={p.id} post={p} onFollow={id=>setPosts(x=>x.map(z=>z.author.id===id?{...z,followingAuthor:true}:z))} onBookmark={id=>setPosts(x=>x.map(z=>z.id===id?{...z,bookmarked:!z.bookmarked}:z))}/>)}</section>}
