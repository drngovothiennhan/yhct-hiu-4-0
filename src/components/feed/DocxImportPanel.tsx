import { useState } from 'react';import { FileText,Sparkles } from 'lucide-react';
export type DocxSuggestion={title:string;summary:string;tags:string[];category?:string;provider?:string};
export default function DocxImportPanel({onApply}:{onApply:(x:DocxSuggestion)=>void}){const [busy,setBusy]=useState(false),[result,setResult]=useState<DocxSuggestion|null>(null),[msg,setMsg]=useState('');const parse=async(file:File)=>{setBusy(true);setMsg('');setResult(null);try{if(!/\.docx$/i.test(file.name))throw new Error('Chỉ hỗ trợ file .docx.');if(file.size>8*1024*1024)throw new Error('File DOCX tối đa 8 MB.');const mammoth=await import('mammoth');const {value}=await mammoth.extractRawText({arrayBuffer:await file.arrayBuffer()});const text=value.replace(/\r/g,'').replace(/\n{3,}/g,'\n\n').trim();if(text.length<80)throw new Error('Tài liệu không đủ nội dung để tóm tắt.');const r=await fetch('/api/ai/docx-summary',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text:text.slice(0,60000),fileName:file.name})});if(!r.ok)throw new Error(`Summarizer HTTP ${r.status}`);const j=await r.json();const out={title:String(j.title||file.name.replace(/\.docx$/i,'')),summary:String(j.summary||''),tags:Array.isArray(j.tags)?j.tags.map(String).slice(0,8):[],category:String(j.category||'Học thuật YHCT'),provider:String(j.provider||'local')};setResult(out);setMsg(`Đã phân tích bằng ${out.provider==='openai'?'A.I server-side':'bộ tóm tắt cục bộ'}. Hãy duyệt trước khi dùng.`)}catch(e){setMsg((e as Error).message)}finally{setBusy(false)}};return <div className="docx-panel"><div className="row"><FileText/><div><b>Tạo nội dung từ Word</b><small>.docx → raw text → tóm tắt → tiêu đề/tags; không tự xuất bản.</small></div></div><label className="upload-inline"><input type="file" accept=".docx,application/vnd.openxmlformats-officedocument.wordprocessingml.document" disabled={busy} onChange={e=>{const f=e.target.files?.[0];if(f)void parse(f)}}/><span>{busy?'Đang đọc DOCX…':'Chọn DOCX'}</span></label>{msg&&<p className="muted">{msg}</p>}{result&&<div className="docx-result"><b>{result.title}</b><p>{result.summary}</p><div className="tags">{result.tags.map(t=><span key={t}>#{t}</span>)}</div><button type="button" onClick={()=>onApply(result)}><Sparkles/>Đưa gợi ý vào bản nháp</button></div>}</div>}
