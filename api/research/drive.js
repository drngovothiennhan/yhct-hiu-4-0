const clean=s=>String(s||'').trim();async function textFor(file,key){const mime=String(file.mimeType||'');let u;if(mime.startsWith('text/')||mime.includes('json')||mime.includes('csv')||mime.includes('markdown')){u=new URL(`https://www.googleapis.com/drive/v3/files/${file.id}`);u.searchParams.set('alt','media')}else if(mime==='application/vnd.google-apps.document'){u=new URL(`https://www.googleapis.com/drive/v3/files/${file.id}/export`);u.searchParams.set('mimeType','text/plain')}else if(mime==='application/vnd.google-apps.spreadsheet'){u=new URL(`https://www.googleapis.com/drive/v3/files/${file.id}/export`);u.searchParams.set('mimeType','text/csv')}else return'';u.searchParams.set('key',key);try{const r=await fetch(u);return r.ok?(await r.text()).slice(0,1_000_000):''}catch{return''}}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const folderId=clean(req.query?.folderId),key=clean(process.env.GOOGLE_DRIVE_API_KEY);if(!/^[A-Za-z0-9_-]{10,200}$/.test(folderId))return res.status(400).json({error:'Folder ID không hợp lệ'});if(!key)return res.status(503).json({error:'Google Drive public RAG chưa được cấu hình server-side. Hãy dùng Local RAG hoặc cấu hình GOOGLE_DRIVE_API_KEY ở Vercel.'});const u=new URL('https://www.googleapis.com/drive/v3/files');u.searchParams.set('q',`'${folderId}' in parents and trashed=false`);u.searchParams.set('fields','files(id,name,mimeType,modifiedTime,webViewLink,size)');u.searchParams.set('pageSize','100');u.searchParams.set('key',key);try{const r=await fetch(u);if(!r.ok)throw new Error(`Google Drive API ${r.status}`);const d=await r.json(),files=(d.files||[]).slice(0,100),textCandidates=files.filter(f=>{const m=String(f.mimeType||'');return m.startsWith('text/')||m.includes('json')||m.includes('csv')||m.includes('markdown')||m==='application/vnd.google-apps.document'||m==='application/vnd.google-apps.spreadsheet'}).slice(0,24),content=new Map();const loaded=await Promise.allSettled(textCandidates.map(async f=>[f.id,await textFor(f,key)]));for(const x of loaded)if(x.status==='fulfilled')content.set(x.value[0],x.value[1]);return res.status(200).json({documents:files.map(f=>({id:f.id,name:f.name,mime:String(f.mimeType||''),text:content.get(f.id)||'',source:'drive',url:f.webViewLink,updatedAt:f.modifiedTime}))})}catch(e){console.error('research-drive',String(e));return res.status(502).json({error:'Không thể đọc Google Drive công khai lúc này'})}}
