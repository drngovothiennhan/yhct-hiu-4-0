import {cloudAiEnabled,cloudAiModel,memberAccess} from './member-access.js';
import {createGeminiWebSearch,geminiAiConfigured,geminiAiModel} from './gemini-provider.js';

const TIMEOUT_MS=20000;
const PUBLIC_TIMEOUT_MS=6500;
const clean=(value,max=2000)=>String(value??'').replace(/[\u0000-\u001f]/g,' ').replace(/\s+/g,' ').trim().slice(0,max);
const safeUrl=value=>{try{const url=new URL(String(value||''));return url.protocol==='https:'?url.toString():''}catch{return''}};
const withTimeout=async(url,options={},timeoutMs=PUBLIC_TIMEOUT_MS)=>{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await fetch(url,{...options,signal:controller.signal})}finally{clearTimeout(timer)}};
function extractText(payload){if(typeof payload?.output_text==='string'&&payload.output_text.trim())return payload.output_text.trim();const parts=[];for(const item of Array.isArray(payload?.output)?payload.output:[])for(const content of Array.isArray(item?.content)?item.content:[])if(typeof content?.text==='string')parts.push(content.text);return parts.join('\n').trim()}
function extractSources(payload){const found=[];for(const item of Array.isArray(payload?.output)?payload.output:[]){for(const content of Array.isArray(item?.content)?item.content:[]){for(const ann of Array.isArray(content?.annotations)?content.annotations:[]){const raw=ann?.url_citation||ann;const url=safeUrl(raw?.url);if(url)found.push({title:clean(raw?.title||url,220),url})}}if(item?.type==='web_search_call'){for(const source of Array.isArray(item?.action?.sources)?item.action.sources:[]){const url=safeUrl(source?.url);if(url)found.push({title:clean(source?.title||url,220),url})}}}return [...new Map(found.map(x=>[x.url,x])).values()].slice(0,6)}
const weatherIntent=/(thời tiết|weather|nhiệt độ|mưa|nắng|bão|dự báo)/i;
const newsIntent=/(tin mới|tin tức|mới nhất|news|điểm tin|tin hôm nay)/i;
const explicitResearchIntent=/\b(pubmed|openalex|doi|pmid|systematic|meta[- ]?analysis|clinical trials?|rct|cohort|case[- ]?control|guideline|evidence)\b|nghiên\s*cứu|y\s*văn|bài\s*báo\s*khoa\s*học|tổng\s*quan\s*hệ\s*thống|phân\s*tích\s*tài\s*liệu|thử\s*nghiệm\s*lâm\s*sàng|bằng\s*chứng|trích\s*dẫn|tài\s*liệu\s*tham\s*khảo|đề\s*cương\s*nghiên\s*cứu/i;
const deepClinicalResearchIntent=/(?:phân\s*tích|đánh\s*giá|so\s*sánh|tổng\s*hợp).{0,80}(?:chẩn\s*đoán|điều\s*trị|dược\s*lý|bệnh\s*học|lâm\s*sàng)|(?:chẩn\s*đoán|điều\s*trị|dược\s*lý|bệnh\s*học|lâm\s*sàng).{0,80}(?:bằng\s*chứng|nghiên\s*cứu|guideline|khuyến\s*cáo|protocol)|phác\s*đồ\s*(?:điều\s*trị|lâm\s*sàng)/i;
export const isResearchIntent=value=>{const text=clean(value,1600);return explicitResearchIntent.test(text)||deepClinicalResearchIntent.test(text)};
const decodeXml=value=>String(value||'').replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g,'$1').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/<[^>]+>/g,'').trim();

function headerLocation(req){const lat=Number(req.headers?.['x-vercel-ip-latitude']),lon=Number(req.headers?.['x-vercel-ip-longitude']);const city=clean(req.headers?.['x-vercel-ip-city']||'',80);return Number.isFinite(lat)&&Number.isFinite(lon)?{lat,lon,label:city||'vị trí hiện tại'}:null}
function explicitPlace(query){const m=query.match(/(?:ở khu vực|ở|tại)\s+([^,.?!]{2,70})/i);return clean(m?.[1]||'',70).replace(/(?:hôm nay|ngày mai|bây giờ|hiện tại)$/i,'').trim()}
async function geocodePlace(name){if(!name)return null;const url=`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(name)}&count=1&language=vi&format=json`;const response=await withTimeout(url,{headers:{accept:'application/json','user-agent':'HIU-YHCT-4.0-XiaoZhi/1.0'}});if(!response.ok)return null;const payload=await response.json().catch(()=>null),hit=Array.isArray(payload?.results)?payload.results[0]:null;const lat=Number(hit?.latitude),lon=Number(hit?.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lon))return null;return {lat,lon,label:[hit?.name,hit?.admin1,hit?.country].filter(Boolean).join(', ')}}
async function weatherReply(req,query){if(!weatherIntent.test(query))return null;const requested=explicitPlace(query),location=requested?await geocodePlace(requested):headerLocation(req);if(!location)return null;const api=`https://api.open-meteo.com/v1/forecast?latitude=${encodeURIComponent(location.lat)}&longitude=${encodeURIComponent(location.lon)}&current=temperature_2m,apparent_temperature,precipitation,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,precipitation_probability_max&forecast_days=2&timezone=auto`;const response=await withTimeout(api,{headers:{accept:'application/json','user-agent':'HIU-YHCT-4.0-XiaoZhi/1.0'}});if(!response.ok)return null;const data=await response.json().catch(()=>null);if(!data?.current||!data?.daily)return null;const temp=Number(data.current.temperature_2m),feels=Number(data.current.apparent_temperature),rainNow=Number(data.current.precipitation||0),rainSum=Number(data.daily.precipitation_sum?.[0]||0),rainProb=Number(data.daily.precipitation_probability_max?.[0]||0),high=Number(data.daily.temperature_2m_max?.[0]),low=Number(data.daily.temperature_2m_min?.[0]);const rainLikely=rainNow>0||rainSum>=0.2||rainProb>=30;const parts=[`Hôm nay tại ${location.label}, ${rainLikely?'có khả năng có mưa':'khả năng mưa thấp'}.`,`Xác suất mưa cao nhất khoảng ${Math.round(rainProb)}%${Number.isFinite(rainSum)?`, tổng lượng mưa dự báo ${rainSum.toFixed(1)} mm`:''}.`];if(Number.isFinite(temp))parts.push(`Hiện khoảng ${temp.toFixed(1)}°C${Number.isFinite(feels)?`, cảm giác như ${feels.toFixed(1)}°C`:''}${Number.isFinite(high)&&Number.isFinite(low)?`; hôm nay ${low.toFixed(0)}–${high.toFixed(0)}°C`:''}.`);return {answer:parts.join(' '),sources:[{title:'Open-Meteo · Dự báo thời tiết',url:'https://open-meteo.com/'}],provider:'open-meteo',degraded:false}}
async function newsReply(query){if(!newsIntent.test(query))return null;const topic=clean(query.replace(/\b(tin mới|tin tức|mới nhất|news|điểm tin|tin hôm nay)\b/gi,' '),140)||'Việt Nam';const rss=`https://news.google.com/rss/search?q=${encodeURIComponent(topic)}&hl=vi&gl=VN&ceid=VN:vi`;const response=await withTimeout(rss,{headers:{accept:'application/rss+xml,application/xml,text/xml','user-agent':'HIU-YHCT-4.0-XiaoZhi/1.0'}});if(!response.ok)return null;const xml=await response.text(),items=[...xml.matchAll(/<item>([\s\S]*?)<\/item>/gi)].slice(0,4).map(match=>{const body=match[1],title=decodeXml(body.match(/<title>([\s\S]*?)<\/title>/i)?.[1]),link=decodeXml(body.match(/<link>([\s\S]*?)<\/link>/i)?.[1]),pubDate=decodeXml(body.match(/<pubDate>([\s\S]*?)<\/pubDate>/i)?.[1]);return {title,link:safeUrl(link),pubDate}}).filter(x=>x.title&&x.link);if(!items.length)return null;const answer=`Các tin nổi bật tôi vừa tìm được về “${topic}”: ${items.map((x,i)=>`${i+1}) ${x.title}${x.pubDate?` (${new Date(x.pubDate).toLocaleDateString('vi-VN')})`:''}`).join(' · ')}`;return {answer,sources:items.map(x=>({title:x.title,url:x.link})),provider:'google-news',degraded:false}}
async function wikipediaReply(query){const searchUrl=`https://vi.wikipedia.org/w/api.php?action=query&list=search&srsearch=${encodeURIComponent(query)}&srlimit=1&format=json&origin=*`;const response=await withTimeout(searchUrl,{headers:{accept:'application/json','user-agent':'HIU-YHCT-4.0-XiaoZhi/1.0'}});if(!response.ok)return null;const data=await response.json().catch(()=>null),hit=data?.query?.search?.[0];if(!hit?.pageid)return null;const extractUrl=`https://vi.wikipedia.org/w/api.php?action=query&prop=extracts&exintro=1&explaintext=1&pageids=${encodeURIComponent(hit.pageid)}&format=json&origin=*`;const detail=await withTimeout(extractUrl,{headers:{accept:'application/json','user-agent':'HIU-YHCT-4.0-XiaoZhi/1.0'}});if(!detail.ok)return null;const payload=await detail.json().catch(()=>null),page=payload?.query?.pages?.[String(hit.pageid)],extract=clean(page?.extract,1800);if(!extract)return null;const title=clean(page?.title||hit.title,180),url=`https://vi.wikipedia.org/?curid=${encodeURIComponent(hit.pageid)}`;return {answer:extract,sources:[{title:`Wikipedia · ${title}`,url}],provider:'wikipedia',degraded:true}}
async function publicDirectReply(req,query){try{const weather=await weatherReply(req,query);if(weather)return weather}catch{}try{const news=await newsReply(query);if(news)return news}catch{}return null}
async function publicFallbackReply(req,query){try{return await wikipediaReply(query)}catch{return null}}

export async function handleXiaoZhiMini(req,res){
  const started=Date.now();res.setHeader('Cache-Control','no-store');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Vary','Authorization');
  if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});
  const access=await memberAccess(req,'member');if(!access.ok)return res.status(access.status).json({error:access.error});
  const query=clean(req.body?.query,1600),pageContext=clean(req.body?.pageContext,1600),localContext=clean(req.body?.localContext,5000);
  if(query.length<2)return res.status(400).json({error:'Query is required'});
  if(isResearchIntent(query))return res.status(200).json({answer:'Học thuật → Trung tâm nghiên cứu',sources:[],provider:'router',degraded:false,route:'research',latencyMs:Date.now()-started});
  const instructions=['Bạn là A.I tìm kiếm công khai mặc định của Trợ lý ứng dụng HIU YHCT 4.0, đồng thời là lớp tương tác giọng nói XiaoZhi.','Dùng Gemini với Google Search làm nguồn công khai mặc định cho các câu hỏi thông thường về HIU, công nghệ, giáo dục, văn hóa, đời sống, giao thông, thời tiết, thể thao, sự kiện và tin tức. Nội dung nghiên cứu/y văn/lâm sàng chuyên sâu phải chuyển sang Trung tâm nghiên cứu thay vì tự xử lý tại A.I Mini.','Ưu tiên dữ liệu mới, nguồn gốc rõ ràng và giữ URL nguồn hợp lệ để giao diện hiển thị. Nếu các nguồn mâu thuẫn, nói ngắn gọn điểm chưa thống nhất thay vì tự suy đoán.','Chỉ dùng LOCAL_CONTEXT để giữ mạch hội thoại hoặc dữ liệu tool đã được ứng dụng cấp cho lượt hiện tại; tuyệt đối không tự giả định rằng kho Drive/tài liệu nội bộ đã được bật.','Không bịa dữ liệu cá nhân hoặc dữ liệu nội bộ.','Trả lời tiếng Việt tự nhiên, đi thẳng vào câu hỏi, súc tích nhưng đủ ý, phù hợp để đọc thành tiếng; không dùng markdown phức tạp.'].join(' ');
  const input=`CÂU HỎI: ${query}\nTRANG HIỆN TẠI: ${pageContext||'không rõ'}\nLOCAL_CONTEXT: ${localContext||'không có'}\nHãy tìm nguồn công khai phù hợp và trả lời trực tiếp.`;
  if(geminiAiConfigured('default')){
    const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);
    try{
      const output=await createGeminiWebSearch({systemInstruction:instructions,prompt:input,signal:controller.signal,mode:'default'}),latencyMs=Date.now()-started;
      res.setHeader('Server-Timing',`xiaozhi;dur=${latencyMs}`);res.setHeader('X-AI-Provider','gemini-web');res.setHeader('X-AI-Model',output.model||geminiAiModel());
      return res.status(200).json({answer:clean(output.text,6500),sources:output.citations,provider:'gemini-web',degraded:false,route:null,latencyMs});
    }catch(error){console.warn(JSON.stringify({event:'xiaozhi_mini',ok:false,provider:'gemini-web',model:geminiAiModel(),role:access.role,latencyMs:Date.now()-started,error:clean(error?.message,180),failover:'openai-web'}))}
    finally{clearTimeout(timer)}
  }
  const direct=await publicDirectReply(req,query);if(direct){const latencyMs=Date.now()-started;res.setHeader('Server-Timing',`xiaozhi;dur=${latencyMs}`);res.setHeader('X-AI-Provider',direct.provider);return res.status(200).json({...direct,route:null,latencyMs})}
  const key=process.env.OPENAI_API_KEY,model=cloudAiModel();
  if(!cloudAiEnabled()||!key||!model){const fallback=await publicFallbackReply(req,query);if(fallback)return res.status(200).json({...fallback,route:null,latencyMs:Date.now()-started});return res.status(200).json({answer:'Các nguồn trực tuyến chưa tìm được kết quả phù hợp cho câu hỏi này. Điểm hoạt động, lịch CLB và điều hướng hệ thống vẫn hoạt động bình thường.',sources:[],provider:'local',degraded:true,latencyMs:Date.now()-started})}
  const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);
  try{
    const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',signal:controller.signal,headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model,store:false,max_output_tokens:1100,instructions,input,tools:[{type:'web_search_preview',search_context_size:'low'}],tool_choice:'auto',include:['web_search_call.action.sources']})});
    if(!response.ok){const detail=await response.json().catch(()=>null);throw new Error(`AI ${response.status}: ${clean(detail?.error?.message||'provider error',160)}`)}
    const payload=await response.json(),answer=clean(extractText(payload),6500);if(!answer)throw new Error('AI returned an empty answer');const sources=extractSources(payload),latencyMs=Date.now()-started;res.setHeader('Server-Timing',`xiaozhi;dur=${latencyMs}`);res.setHeader('X-AI-Provider','openai-web');return res.status(200).json({answer,sources,provider:'openai-web',degraded:false,route:null,latencyMs});
  }catch(error){
    const fallback=await publicFallbackReply(req,query),latencyMs=Date.now()-started;console.warn(JSON.stringify({event:'xiaozhi_mini',ok:Boolean(fallback),role:access.role,latencyMs,error:clean(error?.message,180),fallback:fallback?.provider||null}));if(fallback){res.setHeader('X-AI-Provider',fallback.provider);return res.status(200).json({...fallback,route:null,latencyMs})}return res.status(200).json({answer:'Nguồn A.I tổng hợp và nguồn công khai chưa trả được kết quả cho câu hỏi này. Các chức năng nội bộ vẫn dùng được; hãy thử diễn đạt cụ thể hơn.',sources:[],provider:'local',degraded:true,latencyMs})
  }finally{clearTimeout(timer)}
}
