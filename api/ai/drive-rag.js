import {memberAccess} from '../_lib/member-access.js';
import {buildDriveRagIndex,retrieveDriveRag} from '../_lib/drive-rag.js';
const clean=(v,max=1000)=>String(v??'').replace(/\s+/g,' ').trim().slice(0,max);
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Vary','Authorization');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const access=await memberAccess(req,'member');if(!access.ok)return res.status(access.status).json({error:access.error});const query=clean(req.body?.query,500);if(query.length<2)return res.status(400).json({error:'Query is required'});try{const index=await buildDriveRagIndex();if(!index.configured)return res.status(200).json({ok:true,degraded:true,reason:index.reason,sources:[]});const matches=retrieveDriveRag(query,index.documents,Number(req.body?.limit)||5);return res.status(200).json({ok:true,degraded:false,folder:'HIU YHCT 4.0/Tài liệu nghiên cứu Y học cổ truyền',sources:matches.map(x=>({id:`drive:${x.document.id}`,title:x.document.name,url:x.document.url,text:x.snippet,score:Number(x.score.toFixed(4)),updatedAt:x.document.updatedAt,parentName:x.document.parentName||''}))})}catch(error){console.warn('drive-rag',String(error));return res.status(200).json({ok:true,degraded:true,reason:'drive_unavailable',sources:[]})}}
